@model SearchCarViewModel
<div class="hero" style="background-image: url('/carrentals-master/images/hero_1_a.jpg');">
    <div class="container">
        <div class="row align-items-center justify-content-center">
            <div class="col-lg-10">

                <div class="row mb-5">
                    <div class="col-lg-10 intro">
                        <h1><strong>Car Rental</strong></h1>
                        <br />  
                        <h1>Looking for a vehicle?</h1>
                        <h1> You’re at the right place...</h1>
                    </div>
                </div>
                <form method="post" asp-action="SearchCarList" asp-controller="UIRentaCar" class="trip-form">
                    <div class="row align-items-center">
                        <div class="mb-3 mb-md-0 col-md-3">
                            @Html.DropDownListFor(x => x.ReceivingLocationID, (List<SelectListItem>)ViewBag.receivingLocation, "Receiving Location", new { @class = "custom-select form-control" })
                        </div>
                        <div class="mb-3 mb-md-0 col-md-3">
                            @Html.DropDownListFor(x => x.DestinationLocationID, (List<SelectListItem>)ViewBag.deliveryLocation, "Destination Location", new { @class = "custom-select form-control" })
                        </div>
                        <div class="mb-3 mb-md-0 col-md-3">
                            <div class="form-control-wrap">
                                <input type="text" id="cf-3" placeholder="Receiving Date" asp-for="ReceivingDate" class="form-control datepicker px-3">
                                <span class="icon icon-date_range"></span>

                            </div>
                        </div>
                        <div class="mb-3 mb-md-0 col-md-3">
                            <div class="form-control-wrap">
                                <input type="text" id="cf-4" placeholder="Delivery Date" asp-for="DeliveryDate" class="form-control datepicker px-3">
                                <span class="icon icon-date_range"></span>
                            </div>
                        </div>
                        
                        <div class="mb-3 mb-md-0 col-md-3">
                            <br />
                            <button class="btn btn-primary btn-block py-3">Search Car</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>